# Eval 契约

契约把质量期望变成确定性检查。Harness 支持证据绑定、工具、对话、成本、拒答、安全、角色人格、专家和回归门禁等维度。契约 JSON 会随 Eval 用例保存，并显示在单用例结果徽标和聚合指标中。

证据绑定契约可以要求提示词清单、引用键、命中 ID、充足证据，或明确的证据不足处理。成本契约可以设置预算上限，并要求官方来源价格缺失、过期、SKU 模糊或需要汇率换算时阻塞 USD 汇总。

建议每个风险点先写一个严格契约。例如架构审查必须调用专家，安全测试禁止使用写入工具，常规支持任务限制成本，危险请求必须拒答。契约让跨版本行为可复核，而不是依赖人工印象。
