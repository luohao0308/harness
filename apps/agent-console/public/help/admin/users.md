# 用户与 RBAC

用户管理控制组织成员、角色、API Key 和审计责任。每次请求都会把 JWT 用户和 API Key 主体与当前活跃用户、已接受组织成员关系进行校验。角色会映射到权限，同时保留旧角色标签以兼容早期 Console 页面。

管理员可以邀请和管理成员，工程师可以构建和运行，操作员可以查看运行时状态。具体权限应按组织职责配置，避免把发布、密钥、删除和审计权限集中给不需要的人。

共享 API Key 前先复核成员关系。被撤销用户应通过活跃成员检查失去 JWT 和 API Key 访问。审计日志会记录密钥生命周期、用户变更、留存任务和删除流程，是安全复盘的主要证据。
