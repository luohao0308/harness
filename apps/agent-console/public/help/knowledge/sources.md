# 知识源

知识源保存本地文本、Markdown、导入文档或连接器配置。智能体级知识源只服务一个智能体，组织级知识源可以被同组织多个智能体复用。文档有版本记录，切片会保留来源版本元数据，过期切片会被排除在检索之外。

禁用、启用、归档和删除等生命周期动作都会写入审计证据。交付时要能说明一个回答引用了哪份文档、哪个版本、哪次检索命中，以及哪些证据因为策略或过期状态被省略。

建议使用有标题、来源明确、范围较小的文档。稳定标题、任务示例和无隐藏指令的内容会提升 RAG 质量。导入后运行一次证据绑定查询，并在运行详情检查引用、检索命中、本地状态和提示词清单数量。
