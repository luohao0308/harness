# 扇出与动态扩展

扇出允许一个规划步骤创建多个同级子智能体，用于并行收集证据或执行评审。Harness 会用深度、总数、批次上限、请求者上限和运行中批次守卫限制扇出，避免一次任务无限扩张。

动态扩展只能在同一请求者拥有该批次且配额仍可用时，为活动批次添加同级子智能体。每次扩展都会记录事件，因此后续回放可以解释运行为什么变大、谁发起了扩展、扩展是否命中上限。

当多个专家可以独立工作且不共享可变状态时，使用扇出最合适。不要用扇出来掩盖需求不清。如果父任务缺少验收标准，应先写出更好的计划，再增加专家数量。
