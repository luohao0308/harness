# 专家模板

专家是带有聚焦角色、系统提示词、能力白名单、触发关键词、输出 schema 和预算的子智能体模板。当任务需要有边界的专业判断时，规划器或选择器可以把工作路由给专家。专家输出会单独保存，再链接回父运行。

Eval 可以检查专家契约、必填字段、缺失交接和预算行为。工作区和团队模式中的子智能体调用，都应该留下可复核的专家或子智能体证据，而不是只在父智能体文本中声称“已经调用”。

专家适合重复的专业通道，例如安全审查、架构质询、支持分流、发布验证或研究综合。输出 schema 要小而稳定。好的专家返回父智能体可以使用的结构化证据，而不是一篇难以验收的长文章。
