# 克隆和版本化智能体

克隆会从现有配置创建一个新的智能体，方便团队测试提示词、模型、工具、知识库或预算策略变化，而不影响生产流量。克隆名称应明确，例如 `support-triage-v2-canary`，并在描述中记录本次差异。

历史运行会继续指向原始智能体及其配置证据，克隆版本则可以收集新的 Eval 和可观测性数据。不要通过修改历史配置来“修复”旧运行，应该让每个版本都保留可追踪的行为边界。

推广克隆前，运行原智能体使用的同一批 Eval 数据集，并比较成本、工具调用、证据绑定质量、拒答行为和延迟。回滚时切换默认入口或流量指向旧智能体，而不是覆盖新旧版本的审计记录。
