# 子项目 AGENTS.md 模板

> 将本模板内容复制到确实需要局部规则的子项目根目录，并命名为 `AGENTS.md`。不要把本文件本身当成运行规则，也不要复制根 `AGENTS.md` 的通用核心。

```markdown
# <!-- 子项目名称 -->局部协作规则

本文件只约束当前目录及其子目录。全局安全、文档治理、Git 和完成标准继续继承仓库根 `AGENTS.md`。

## 范围与所有权

- 负责路径：
- 不负责路径：
- 修改需要确认的区域：

## 技术与目录约定

- 语言/框架/版本：
- 入口与模块结构：
- 依赖方向和禁止模式：

## 开发命令

| 目的 | 命令 | 工作目录 |
|---|---|---|
| 安装/启动 |  |  |
| 定向测试 |  |  |
| lint/type/build |  |  |

## 局部验证与文档同步

- 改动必须运行：
- 运行时变化后的重启/冒烟：
- 需要同步的架构、契约或 Runbook：

## 局部禁止操作

- <!-- 只写本子项目特有禁区，不能解除根规则 -->
```

局部 `AGENTS.md` 只写差异。多个工具需要项目入口时，由工具指向同一规则源，不复制成多份模型专属文件。
